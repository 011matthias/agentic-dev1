# Product brief: {{PRODUCT_TITLE}}

Fill this before writing code. It is the spec the acceptance criteria and the
verify profile key off. Keep it to what is decided; delete a line rather than
leave it as a question.

## What

One paragraph: the thing this product is, in plain terms. What does a user do
with it.

## Who

The person it is for, and the one moment it has to nail for them.

## Success metric

The single measured signal that says it works (CREW's pattern: measurement is
first-class, logged from session one, not bolted on later). Name the signal, the
threshold, and where it is recorded. Example shape: "median session >= 15 min,
logged to localStorage `{{PRODUCT_NAME}}:session`."

## Scope cut

What this version deliberately does NOT do. The cut is the spec: it is what keeps
the first build shippable.
