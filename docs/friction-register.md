# Friction Register

Self-annealing log for agentic-dev1. One row per friction event: something the owner
had to correct, something the agent should have automated, or a gate that should have
fired and did not. `tools/friction-watch.py` reads this table at session start and
surfaces concentration / recurrence / stale patterns.

Types: `agent-deferred`, `missed-tool`, `verification-theater`, `skipped-gate`,
`over-literal`, `strategic-gap`, `scope-creep`, `slow-path`, `missed-memory-recall`,
`infrastructure-deferred`.

| Date | Scope | Type | Description | Resolved | Fix |
|------|-------|------|-------------|----------|-----|
| 2026-06-07 | harness | infrastructure-deferred | seeded gate-skip-detector + post-action-gate don't recognize tools/verify.py as a validation step, so a push after verify.py false-fires the pre-publish advisory in dev1 sessions | Yes (PR #8) | Cycle 2: verify.py added to gate-skip VALIDATE_PATTERNS + READONLY_PATTERNS and to post-action BUILD_TEST_PATTERNS + EXEMPT_PATTERNS (recognized as verification, exempt from the 3x streak). Pinned by tools/tests/test_gate_skip_and_post_action.py incl. end-to-end push-after-verify check |
| 2026-06-09 | harness | slow-path | dev1 build session launched from the agentic-ops repo: ops PreToolUse hooks resolve `.claude/hooks/*` by cwd-relative path, so a `cd` into dev1 made the ops-only hooks (instantly-invasive-gate, reference-anchor-gate) fail-closed and dead-lock both Bash and Write/Edit; ~5 calls to diagnose | Yes (workaround) | pin the harness cwd to the launch repo (Set-Location is shared across tools) and drive the target by absolute paths + git -C / npm --prefix / uv run --directory, never cd-ing in. Structural kill: launch dev1 sessions from ~/Repo/agentic-dev1 so dev1's own hooks resolve and its floor gate actually fires. Promote to a gotcha memory if it recurs |
| 2026-06-09 | harness | slow-path | RECURRENCE of the row above (cycles 2-5.1 build session): a `cd <dev1> ; gh pr create ...` slipped into one command and leaked the shell cwd into dev1 (gh -R needs no cwd, so the cd was needless). Self-caught, recovered via PowerShell Set-Location; no deadlock this time. cd-guard only matches the `cd X && ...` form, not `cd X ; ...`, so it did not fire | Yes | cd-guard widened (2026-06-16): the path-boundary trailing group required a space before `;`, so it caught `cd X ; cmd` but not the no-space `cd X; cmd`. Replaced with a zero-width `(?=[\s;&|]|$)` lookahead, so both `;`/`|`/`&` forms are blocked with or without a leading space (main()'s `rest` check still distinguishes bare vs chained). Pinned by test_cd_guard semicolon cases (spaced + tight). Carried reflex unchanged: git -C / gh -R / uv run --directory / absolute paths, never a leading cd. See [[gotcha_cross_repo_cwd]] |
| 2026-06-16 | harness | slow-path | PowerShell 5.1 native-arg quoting mangled two invocations from the Desktop-rooted session: a `gh ... --jq` expression with embedded double quotes (jq received a bare regex, parse error), and a `git commit -m @'...'@` here-string whose message contained double quotes (the message split into pathspecs, commit failed). Each was self-caught with one retry. | Yes | Reflex for PowerShell-driven git/gh: filter JSON in PowerShell (`ConvertFrom-Json` + `Where-Object`) instead of `gh --jq` with a quoted regex; pass commit messages and PR bodies via files (`git commit -F <file>`, `gh --body-file <file>`), never `-m`/`-b` with embedded quotes. Promote to a gotcha memory if it recurs. |
| 2026-06-16 | harness | slow-path | A parallel Cycle-7 session sharing THIS working tree ran `git checkout -b harness/cycle-7-spine` mid-build; the shared HEAD moved and this session's `skil_crew-verify` commit landed on that branch instead of its own `product/crew/verify-skill`. Caught via reflog + the tell that untracked `templates/` + `tools/scaffold.py` changed mtime mid-session. ~4 calls to diagnose + recover | Yes | Recovered with no data loss: `git push origin <sha>:refs/heads/<my-branch>` to immortalize first (no local change), then a guarded disentangle (re-check the wrong branch's tip, `branch -f` my branch to the commit, `switch`, restore their branch to its base). Staged only explicit paths so the WIP was never swept. Promoted to gotcha memory [[gotcha_shared_worktree]]. Structural fix: a separate `git worktree` per concurrent session (cannot be hooked from inside one session; owner-level operating discipline) |
| 2026-06-16 | harness | skipped-gate | post-action / gate-skip 3x iteration cap fired on intentional mutation-test reruns of `products/crew/verify/round.py` (three DISTINCT injected violations to prove the verifier bites), misreading verification-of-a-test as a fix-then-test grind | Partial | Advisory only (no block), low harm. The gate cannot tell "same command, different injected mutation" from "grinding one failure". Candidate anneal: recognize an ad-hoc verify/test script the way `verify.py` is already exempted from the streak, or detect that the working tree changed between reruns (a mutation cycle) vs unchanged (a true retry). Logged; not yet mechanized |
| 2026-06-16 | harness | missed-tool | a freshly-written `.claude/agents/design-reviewer.md` could not be invoked by `subagent_type` in the same session that authored it (Agent tool: "agent type not found"); even the existing `code-reviewer` was absent from the registry. Project agents load at session START, so a same-session author-then-invoke is not possible | Yes | Validated the agent's METHOD inline instead (drove its boot -> screenshot -> read -> judge pipeline on the running CREW build, produced a real review), which is the substantive check anyway; the agent type itself is invocable from the next session start. Reflex: when authoring an agent, verify its method on a real target inline; do not expect to spawn it by type until a restart. Memory candidate if it recurs |
| 2026-06-17 | harness | skipped-gate | the gate-skip-detector "pre-publish" advisory fired on docs-only PR ships (#31, #32) because no `verify.py` ran in the recent buffer; for a pure-docs diff the real gate is CI, and a harness-scope verify adds little signal, so the nudge is noise here | Partial | Advisory only (no block), low harm. Candidate anneal: exempt a publish whose staged diff is markdown-only (or docs/ + memory/ only) from the pre-publish nudge, the way verify.py reruns are already exempted from the streak. Logged; not yet mechanized |
| 2026-06-16 | harness | slow-path | INTRA-repo variant of the cwd/hook-resolution family (rows above): a mutation-test command led with `cd templates/website && node -e ...`, leaking the shell cwd into a repo SUBDIR that has no `.claude/`; the next call's cwd-relative hooks (cd-guard PreToolUse, post-action/pressure PostToolUse) could not self-locate and errored. cd-guard did not fire on the leading `cd <subdir> && ...` because the target is inside the project root (its block targets cross-cwd leakage, not an in-repo descend). ~3 calls to diagnose + recover | Yes | Recovered via PowerShell `Set-Location <root>` (shared cwd across tools), then drove the rest by `npm --prefix` / `git -C` from root, never cd-ing in. This is the 4th instance of the same root cause (rows 2026-06-09 x2, the parallel-worktree row, now this). Structural kill is overdue and already named in rule_behaviors' hook contract: resolve hook script paths by `CLAUDE_PROJECT_DIR` / script-relative, not cwd-relative, so a subdir cwd never breaks them. Carried reflex unchanged. See [[gotcha_cross_repo_cwd]] |
