# Checkpoint 2026-06-16: Cycle 6 harvest (vendoring) + cd-guard anneal

Scope: harness | Band: autonomous (driven from a Desktop-rooted session by
absolute paths + git -C / gh -R / uv run --directory; dev1 hooks were inactive
this session, so the floor was self-enforced).

## Done (this arc, all merged on CI-green, floor untouched)
- **Workspace bring-up** from a Desktop-rooted session per `rule_session_start`:
  loaded all memory, ran friction-watch, checked branch/PR state and
  `wire-hooks --check` (dev1 wiring intact, 9/9).
- **PR #17**: kept and merged the dangling `agentic-dev1.code-workspace` file
  (owner decision), resolving the contradictory "added in HEAD, deleted in
  working tree" state.
- **PR #18 (Cycle 6 harvest; CURATION actions 2 + 3)**: vendored
  `webapp-testing` and `frontend-design` from anthropics/skills (Apache-2.0,
  pinned `5754626`). Every vendored file is byte-identical to its upstream blob
  SHA (verified pre- and post-commit; autocrlf left the objects LF). Each skill
  ships its LICENSE.txt plus a VENDORED.md (source, pinned SHA, usage). Paired
  anneal: `em-dash-strip-gate` now skips any `.claude/skills/<skill>/` dir that
  ships a LICENSE.txt, so vendored third-party markdown stays byte-faithful (the
  gate docstring already intended to exclude vendored content; this was the first
  vendored markdown in the tree). 3 tests added.
- **PR #19 (anneal)**: widened `cd-guard` to the no-space `cd X;` chain form. The
  spaced `cd X ;` form was already caught by the old regex; only the tight
  no-space form slipped (confirmed by a failing test before the fix). Replaced
  the space-requiring trailing group with a zero-width `(?=[\s;&|]|$)` boundary
  lookahead. Closed the 2026-06-09 cross-repo-cwd recurrence friction row and
  corrected `gotcha_cross_repo_cwd` + `MEMORY.md` (the watch-by-hand note was
  obsolete). 2 tests added.

## Current state (main)
- main HEAD `c4575c0` (#19). No open PRs. Friction register: 3 rows, 0
  unresolved (friction-watch: no signals, first fully clear state since
  scaffolding).
- Skills: CURATION.md, skil_game-feel-review (authored), webapp-testing +
  frontend-design (vendored). Rules / agents / commands unchanged since
  2026-06-09.
- Verify: the enforcement suite + ruff + check-index ran green locally (CI
  parity) and in CI on each PR; crew build green on every PR. `verify.py all`
  was not re-run (no product code touched); last recorded PASS 5/5 at #11.

## Next steps (blueprint Cycle 6 tail -> Cycle 7; CURATION action list)
1. **New-slot GitHub harvest sweep**: website starters, scaffolding / generator
   patterns, deploy-shipping workflows, design-taste rubrics, agent precedents.
   The blueprint specs this as a Workflow fan-out (multi-agent); it needs an
   explicit opt-in before launching.
2. **skil_crew-verify** (CURATION action 4, now unblocked by webapp-testing):
   drive a real pass-and-play CREW round via webapp-testing and assert the
   `rule_code_review` invariants (Confessions anonymity, no StrictMode
   double-fire, pure-CSS press-squash, measurement schema, localStorage keys +
   snapshot v); register it as products/crew's npm `verify` so verify.py
   auto-discovers it. Closes the Cycle-4 anonymity-has-no-test anneal. Best run
   from a dev1-rooted session (it runs the product headless).
3. **Cycle 7 (spine)**: `templates/` (website / app / game) + `scaffold.py` +
   `comd_new-product` + the dev1 manifest block + per-archetype verify wiring +
   product-discovering CI.

## Decisions
- Vendored both skills in ONE PR (mechanical, same source and license) rather
  than two CI cycles; webapp-testing stays flagged as the keystone in CURATION.
- Paired the em-dash exemption with the vendoring because that change introduced
  the first vendored markdown into the gate's scope (fix the category where it is
  created, not later).
- Stayed inside the autonomous band: nothing outward beyond the existing public
  repo, and did not auto-launch the Cycle-6 Workflow (multi-agent orchestration
  needs an explicit opt-in).

## Strategic feedback
- Worked: the lean-autonomous loop ran clean across three merges (branch ->
  verify -> PR -> CI-green -> squash-merge -> prune), floor untouched, entirely
  from a Desktop-rooted session driven by git -C / gh -R / uv run --directory.
- Suggestion: launch the next dev1 session from `~/Repo/agentic-dev1` so the
  hooks are live (this session self-enforced the floor) and product runs are
  natural.
- Health: friction register fully clear; two self-corrected PowerShell native-arg
  quoting hiccups logged as a slow-path row with the file-based reflex.
