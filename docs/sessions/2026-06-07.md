# Session 2026-06-07: agentic-dev1 spinout + grounding

## What landed

- **Scaffold** (public repo `011matthias/agentic-dev1`): seeded the
  domain-agnostic self-annealing harness from agentic-ops (9 enforcement hooks,
  `wire-hooks`, CI-gated pytest + ruff + INDEX gate, the tiered no-auto-commit
  gate), dropped the consultancy layer, added the product-hub shape. CREW migrated
  into `products/crew/` (full phase-1 work including the juice pass, verified
  byte-identical), and `crew-sandbox` deleted.
- **Foundation** (PR #1, merged): `rule_dev_loop` (build spine + 3
  non-negotiables), `tools/verify.py` (the executable verify gate), `rule_operating`
  (lean, floor-gated autonomy), `rule_session_start` plus the
  `comd_resume` / `comd_checkpoint` commands (full session protocol).
- **Comprehension** (PR #2, merged): `products/crew/ARCHITECTURE.md`, traced to
  source.

## How we work (decided this session)

Lean autonomous: I act on and land everything reversible, including drafting and
merging rules and docs; only the irreversible floor (deploy, release, tag, force,
direct-to-main, destructive/outward-facing) needs an explicit order. Full session
protocol. Source of truth: `rule_operating` + `rule_session_start`.

## Current state

- `main` @ `5bdf932`: scaffold + foundation + CREW architecture. `verify.py all`
  PASS 4/4. No open PRs, no in-flight branches.

## Open items

- **ci.yml not on the remote.** The `gh` OAuth token lacks `workflow` scope. Grant
  once with `gh auth refresh -h github.com -s workflow`, then push `ci.yml`; until
  then the lean-autonomous merge runs on local `verify.py` instead of CI-green.
- **Anneal queued.** The seeded `gate-skip-detector` (and `post-action-gate`) do
  not recognize `tools/verify.py` as a validation step, so a push after `verify.py`
  false-fires the pre-publish advisory in dev1 sessions. Logged in the friction
  register.

## Next steps

- Re-point the inert seeded-hook policy when writing `rule_anti_slop`: em-dash
  scope (to `docs/` + product markdown), the no-auto-commit prototype carve-out,
  the gate-skip MCP patterns, and `verify.py` recognition.
- The remaining product-dev rules as friction earns them: `rule_code_review`,
  `rule_release`, `rule_dependency_hygiene`; the agent set (a code-reviewer first).

## Feedback

Worked: dogfooding the foundation the moment it landed (verify.py gating its own
PR, the architecture doc as the first comprehension exercise) made the rules
concrete rather than aspirational. Health: the ci.yml workflow-scope gap is the one
thing between here and a fully hands-off merge loop; closing it is high-value.
