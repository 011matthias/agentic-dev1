# Checkpoint 2026-06-09: grounding + CI live + ops-decouple (round 1)

Scope: harness | Band: autonomous

## Done (this arc)
- Scaffolded agentic-dev1 as a separate public repo from the ops harness; CREW
  migrated into `products/crew/` (uncommitted juice preserved, build verified);
  `crew-sandbox` deleted (15 stale vite/esbuild processes were pinning it).
- Operating foundation written + merged (#1): `rule_dev_loop` (build spine + 3
  non-negotiables), `rule_operating` (lean autonomous, floor-gated),
  `rule_session_start` + `comd_resume`/`comd_checkpoint`, and `tools/verify.py`
  (the verify-behavior gate).
- CI live + green: workflow scope granted via a hand-driven GitHub device flow
  (gh's own flow kept deadlining with no TTY), `ci.yml` created on main via the
  Contents API with a workflow-scoped token; run green (hooks + crew jobs).
- Runtime state decoupled from ops (#4): every shared system-temp / env-var
  identifier namespaced `agentic-ops-*` -> `agentic-dev1-*`. Auto-merged on
  CI-green with no prompt; first proof the lean-autonomous loop works end to end.

## Current state (main)
- main HEAD `93f1157`. `verify.py all` -> PASS 4/4. CI green.
- `ci.yml` landed via a one-off workflow-scoped device-flow token (now wiped from
  disk). gh's stored token still lacks `workflow` scope, so a future `ci.yml`
  edit needs a re-mint or a proper `gh auth refresh -s workflow`.

## Next steps
1. **The big build**: `docs/architecture-build-prompt.md`: design + build the
   best-possible game/app-dev architecture (rules, commands, agents, and
   especially GitHub-sourced skills). Run it in a fresh dev1 session.
2. **Finish the ops-decouple**, paired with rule authoring: write dev1's
   `rule_behaviors` + `rule_anti_slop`, and in the same change re-point the hooks'
   advisory rule-name citations + strip inert ops domain patterns (Make/n8n MCP,
   vercel/railway, local-web carve-out, client em-dash scope). One hook per rule.
3. **Comprehension layer**: `products/crew/ARCHITECTURE.md` + an
   understand-before-change gate.
4. **Queued friction** (already in the register, `infrastructure-deferred`): teach
   `gate-skip-detector` + `post-action-gate` to recognize `tools/verify.py` as a
   validation step so a push after `verify.py` stops false-firing the pre-publish
   advisory.

## Health
The CI grounding paid off immediately: #4 auto-merged the moment CI went green,
zero prompts. That is the loop the operating model was built around.

---

# Checkpoint 2026-06-09 (round 2): architecture build-out cycles 2 to 4

Scope: harness | Band: autonomous (driven from the agentic-ops1 repo by absolute
paths + git -C / gh -R / uv run --directory; floor self-enforced).

## Done (this arc, all merged on CI-green)
- **Cycle 2** (PR #8, #9): `rule_behaviors.md`, the decision-time-gate rule the
  seeded advisory hooks cite by name (B1 search-before-ask, input interpretation,
  question-the-approach, B2 verify, B3 suspect-own-change, build escalation, ship
  gate, friction logging, fail-open hook contract). Curated dev1-native:
  cross-references `rule_operating` + `rule_dev_loop` rather than restating; the
  consultancy B4/B5/batch-manifest/instance-resolution gates not ported. Paired
  re-point: `gate-skip-detector` + `post-action-gate` now recognize
  `tools/verify.py` as a validation/verification step (closing the 2026-06-07
  friction row), stripped the inert ops patterns (Make/n8n MCP, vercel), and
  re-pointed citations (ITERATION-LOOP.md -> rule_behaviors). Test:
  `test_gate_skip_and_post_action.py` (10 tests, 4 end-to-end with controls).
- **Cycle 3** (PR #10): `rule_anti_slop.md` (signal-density framing for the
  internal knowledge base + code-comment WHY-not-WHAT discipline). Re-pointed
  `em-dash-strip-gate` scope from inert client paths to dev1 markdown
  (`in_dev1_doc_scope`); fixed `strip-em-dash.py` to stop converting ` -- ` (a
  legitimate CLI separator in this repo, the dev1 divergence) and strip only the
  em-dash character; cleaned 20 em-dashes from the seed docs (CLI ` -- `
  separators verified intact). Test re-pointed + ` -- `-preservation pin added.
- **Cycle 4** (PR #11): the Review phase made concrete. `rule_code_review.md`
  (five lenses + severity-banded merge gate), `code-reviewer.md` (read-only
  adversarial agent), `comd_review.md`. Exercised on a real CREW diff: a planted
  `answers: result` edit in Confessions' reveal payload; the reviewer caught it
  as one grounded blocker (anonymity invariant + full persist/remote/export blast
  path) and surfaced the test-adequacy gap unprompted. Fixture reverted.

## Current state (main)
- main HEAD `e7942d5` (#11). `verify.py all` -> PASS 5/5. CI green. No open PRs.
- Rules now: rule_dev_loop, rule_operating, rule_session_start, rule_behaviors,
  rule_anti_slop, rule_code_review. Agents: code-reviewer. Commands: comd_resume,
  comd_checkpoint, comd_review.

## Next steps
1. **Cycle 5 (the big leverage): skills research + curation.** Workflow fan-out
   over real GitHub sources (verify each exists/maintained: anthropics/skills,
   hesreallyhim/awesome-claude-code, VoltAgent/awesome-claude-code-subagents,
   wshobson/agents, davila7/claude-code-templates). Per slot (web/app,
   mobile/native, games, cross-cutting SWE): read, judge quality+fit+license,
   vendor/adapt the best into `.claude/skills/` with attribution, reject the rest,
   log all in `.claude/skills/CURATION.md`. Where nothing good exists, write one
   (a CREW-grounded game-feel review skill). Multiple PRs.
2. **Cycle 6:** remaining commands (comd_new-product, comd_build,
   comd_ship/comd_release, comd_playtest [CREW localStorage measurement model is
   the reference], comd_status), rules (rule_testing, rule_product_structure,
   rule_release, rule_dependency_hygiene), agents (verifier, architect/planner,
   debugger, game-feel, ui-ux + accessibility) as each earns its place.

## Anneal follow-ups surfaced this arc
- **CREW anonymity invariant has no test** (found by the Cycle-4 review exercise).
  Turn it into a guard under Cycle 6 `rule_testing`: a test asserting Confessions
  never emits a per-player vector in any logged event. The review caught it; the
  point of anneal is that the build should too.
- **cd-guard gap (cross-repo):** `cd X ; ...` (semicolon) leaks the shell cwd the
  same way `cd X && ...` does, but cd-guard only matches the `&&` form. In an
  ops-rooted dev1 session this risks the documented cwd deadlock. Near-miss this
  session (recovered via PowerShell Set-Location). Structural kill already in the
  register (launch dev1 sessions from the dev1 root); widening cd-guard to the
  `;` form is the smaller follow-up if it recurs.

## Health
The lean-autonomous loop ran clean for three full cycles: branch -> verify ->
PR -> CI-green -> squash-merge, no prompts, floor untouched. The Cycle-2 verify.py
recognition fix is already paying off in dev1 sessions (a push after verify.py no
longer false-fires). The one friction was self-inflicted and cross-repo (the cd
into dev1), not a loop failure.

---

# Checkpoint 2026-06-09 (round 3): Cycle 5.1 skills curation

Scope: harness | Band: autonomous.

## Done
- **Cycle 5 increment 1** (PR #13): the skills-curation research + first build.
  A Workflow fan-out (5 source-verify agents over the five named GitHub repos,
  all confirmed real; 4 per-slot evaluators; 1 synthesis) produced the decision.
  Landed `.claude/skills/CURATION.md` (the auditable evaluated/kept/rejected log
  across web/app, mobile/native, games, cross-cutting SWE) and the authored
  `.claude/skills/skil_game-feel-review/SKILL.md` (CREW-grounded, 7-dimension feel
  rubric, gstack-MIT-attributed, gstack plumbing stripped).
- Curation result: 2 vendored (webapp-testing + frontend-design, Apache-2.0), 3
  authored, the rest rejected (generic / duplicative / wrong-engine / wrong-phase
  / unlicensed); native-iOS bookmarked.

## Current state (main)
- main HEAD `0c6cc08` (#13). `verify.py all` -> PASS 5/5. CI green. No open PRs.
- Skills: CURATION.md + skil_game-feel-review. Rules: dev_loop, operating,
  session_start, behaviors, anti_slop, code_review. Agents: code-reviewer.
  Commands: comd_resume, comd_checkpoint, comd_review.

## Next steps (the CURATION.md action list is the source of truth)
1. **Vendor `webapp-testing`** (anthropics/skills, Apache-2.0) into
   `.claude/skills/webapp-testing/` with the Apache NOTICE; black-box (invoke
   via `--help`, do not ingest). HIGHEST LEVERAGE: closes the build-only verify
   gap (products/crew has no npm `verify` script). It is the keystone
   skil_game-feel-review + skil_crew-verify compose with.
2. **Vendor `frontend-design`** (same source/license) with attribution.
3. **Author `skil_crew-verify`**: drives a full pass-and-play round via
   webapp-testing, asserts the rule_code_review invariants (Confessions
   anonymity, no StrictMode double-fire, pure-CSS press-squash, measurement
   schema, localStorage keys + snapshot v), registers as products/crew npm
   `verify` so verify.py auto-discovers it. **Closes the Cycle-4 anneal** (the
   anonymity invariant has no test). Pairs with Cycle 6 `rule_testing`.
4. **Author `skil_pwa-install-and-measure`** (deferred; build when the install /
   measurement work is scheduled).
5. **Cycle 6:** remaining commands (comd_new-product, comd_build,
   comd_ship/comd_release, comd_playtest, comd_status), rules (rule_testing,
   rule_product_structure, rule_release, rule_dependency_hygiene), agents
   (verifier, architect/planner, debugger, game-feel runner, ui-ux +
   accessibility) as each earns its place.

## Health
Five cycles shipped this session (2, 3, 4, checkpoint, 5.1) plus a 10-agent
research workflow, all merged on CI-green, floor untouched. Stopped at critical
context pressure with the remaining Cycle-5 vendoring + Cycle 6 fully specified
in CURATION.md and above, rather than cramming them into a degraded-context tail.
Resume here with a fresh budget; the keystone next action is vendoring
webapp-testing.
